---
phase: 01-foundation-schema
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - supabase/migrations/00019_create_trust_functions.sql
  - supabase/migrations/00020_enable_rls.sql
  - supabase/migrations/00021_rls_users_profiles_photos.sql
  - supabase/migrations/00022_rls_interactions.sql
  - supabase/migrations/00023_rls_messaging.sql
  - supabase/migrations/00024_rls_safety_system.sql
  - src/types/database.types.ts
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "is_blocked(user_a, user_b) returns true when either user has blocked the other"
    - "shares_school(user_a, user_b) returns true when both users share at least one school"
    - "RLS is enabled on all user-facing tables"
    - "Authenticated users can read their own data on all tables"
    - "Profiles of users from different schools are not visible (shared-school gating)"
    - "Blocked users are invisible across all visibility queries"
    - "TypeScript types are generated from the live Supabase schema"
  artifacts:
    - path: "supabase/migrations/00019_create_trust_functions.sql"
      provides: "is_blocked() and shares_school() SECURITY DEFINER functions"
      contains: "SECURITY DEFINER"
    - path: "supabase/migrations/00020_enable_rls.sql"
      provides: "RLS enabled on all tables"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "src/types/database.types.ts"
      provides: "Generated TypeScript types matching live schema"
      contains: "export type Database"
  key_links:
    - from: "supabase/migrations/00021_rls_users_profiles_photos.sql"
      to: "supabase/migrations/00019_create_trust_functions.sql"
      via: "RLS policies reference is_blocked and shares_school functions"
      pattern: "public\\.is_blocked|public\\.shares_school"
    - from: "src/lib/supabase.ts"
      to: "src/types/database.types.ts"
      via: "Generic type parameter on createClient"
      pattern: "createClient<Database>"
---

<objective>
Create shared trust functions (is_blocked, shares_school), enable Row Level Security on all tables with shared-school gating policies, and generate TypeScript types from the live schema.

Purpose: This is the security foundation. Without RLS and trust functions, the database is unprotected. Without generated types, the client has no type safety against the schema. The PRD mandates server-side enforcement of shared-school visibility and block rules — this plan implements those guarantees at the database level.

Output: All tables protected by RLS, trust functions callable from policies and Edge Functions, and auto-generated TypeScript types for the Supabase client.
</objective>

<execution_context>
@/Users/bzcruz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bzcruz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@.planning/phases/01-foundation-schema/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trust functions and enable RLS on all tables</name>
  <files>
    supabase/migrations/00019_create_trust_functions.sql,
    supabase/migrations/00020_enable_rls.sql,
    supabase/migrations/00021_rls_users_profiles_photos.sql,
    supabase/migrations/00022_rls_interactions.sql,
    supabase/migrations/00023_rls_messaging.sql,
    supabase/migrations/00024_rls_safety_system.sql
  </files>
  <action>
    Create and apply migrations for trust functions and RLS policies. Use Supabase MCP `apply_migration` tool for each.

    **Migration 19: Trust Functions** (`00019_create_trust_functions.sql`)

    Create two SECURITY DEFINER functions (bypass RLS on lookup tables, STABLE for optimizer caching):

    ```sql
    -- is_blocked: bidirectional block check
    CREATE OR REPLACE FUNCTION public.is_blocked(user_a uuid, user_b uuid)
    RETURNS boolean
    LANGUAGE plpgsql
    SECURITY DEFINER
    STABLE
    AS $$
    BEGIN
      RETURN EXISTS (
        SELECT 1 FROM public.blocks
        WHERE (blocker_id = user_a AND blocked_id = user_b)
           OR (blocker_id = user_b AND blocked_id = user_a)
      );
    END;
    $$;

    -- shares_school: checks if two users share at least one school
    CREATE OR REPLACE FUNCTION public.shares_school(user_a uuid, user_b uuid)
    RETURNS boolean
    LANGUAGE plpgsql
    SECURITY DEFINER
    STABLE
    AS $$
    BEGIN
      RETURN EXISTS (
        SELECT 1 FROM public.user_schools us1
        INNER JOIN public.user_schools us2 ON us1.school_id = us2.school_id
        WHERE us1.user_id = user_a AND us2.user_id = user_b
      );
    END;
    $$;
    ```

    **Migration 20: Enable RLS** (`00020_enable_rls.sql`)

    Enable RLS on ALL user-facing tables:
    users, profiles, photos, user_schools, likes, matches, dismissals, saves, threads, messages, blocks, reports, enforcement_actions, ranking_config, ads_engagement, subscriptions

    Also enable on schools (public read, no write by users).

    **Migration 21: RLS for users, profiles, photos** (`00021_rls_users_profiles_photos.sql`)

    CRITICAL performance pattern: Always use `(select auth.uid())` wrapper for initPlan caching (see RESEARCH.md Pitfall 2). Always specify `TO authenticated`.

    Users table:
    - SELECT own: `user_id = (select auth.uid())`
    - SELECT shared school: `shares_school((select auth.uid()), id) AND NOT is_blocked((select auth.uid()), id)` — wrap function calls in `(select ...)` too
    - UPDATE own: `id = (select auth.uid())`
    - INSERT own: `id = (select auth.uid())`

    Profiles table:
    - SELECT own: `user_id = (select auth.uid())`
    - SELECT shared school: `user_id != (select auth.uid()) AND (select public.shares_school((select auth.uid()), user_id)) AND NOT (select public.is_blocked((select auth.uid()), user_id))`
    - INSERT own: `user_id = (select auth.uid())`
    - UPDATE own: `user_id = (select auth.uid())`

    Photos table:
    - SELECT own: `user_id = (select auth.uid())`
    - SELECT shared school (same pattern as profiles)
    - INSERT own: `user_id = (select auth.uid())`
    - UPDATE own: `user_id = (select auth.uid())`
    - DELETE own: `user_id = (select auth.uid())`

    Schools table:
    - SELECT: allow all authenticated users (public reference data)
    - No INSERT/UPDATE/DELETE for regular users

    User_schools table:
    - SELECT own: `user_id = (select auth.uid())`
    - SELECT: allow reading other users' schools for shared-school checks in app layer
    - INSERT own: `user_id = (select auth.uid())`
    - DELETE own: `user_id = (select auth.uid())`

    **Migration 22: RLS for interaction tables** (`00022_rls_interactions.sql`)

    Likes:
    - SELECT own (sent): `liker_id = (select auth.uid())`
    - SELECT received (for Liked Me feature): `liked_id = (select auth.uid())`
    - INSERT own: `liker_id = (select auth.uid())`
    - DELETE own: `liker_id = (select auth.uid())`

    Matches:
    - SELECT participant: `user_a_id = (select auth.uid()) OR user_b_id = (select auth.uid())`
    - No direct INSERT/UPDATE/DELETE by users (handled by server functions)

    Dismissals:
    - SELECT own: `dismisser_id = (select auth.uid())`
    - INSERT own: `dismisser_id = (select auth.uid())`

    Saves:
    - SELECT own: `saver_id = (select auth.uid())`
    - INSERT own: `saver_id = (select auth.uid())`
    - DELETE own: `saver_id = (select auth.uid())`

    **Migration 23: RLS for messaging** (`00023_rls_messaging.sql`)

    Threads:
    - SELECT participant: `user_a_id = (select auth.uid()) OR user_b_id = (select auth.uid())`
    - No direct INSERT by users (created by match system)

    Messages:
    - SELECT: user is participant in the thread: `EXISTS (SELECT 1 FROM public.threads t WHERE t.id = thread_id AND (t.user_a_id = (select auth.uid()) OR t.user_b_id = (select auth.uid())))`
    - INSERT: user is participant and sender matches auth: `sender_id = (select auth.uid()) AND EXISTS (SELECT 1 FROM public.threads t WHERE t.id = thread_id AND (t.user_a_id = (select auth.uid()) OR t.user_b_id = (select auth.uid())))`

    **Migration 24: RLS for safety/system tables** (`00024_rls_safety_system.sql`)

    Blocks:
    - SELECT own: `blocker_id = (select auth.uid())`
    - INSERT own: `blocker_id = (select auth.uid())`
    - DELETE own: `blocker_id = (select auth.uid())`

    Reports:
    - SELECT own: `reporter_id = (select auth.uid())`
    - INSERT own: `reporter_id = (select auth.uid())`

    Enforcement_actions:
    - SELECT own: `user_id = (select auth.uid())` (user can see their own enforcement history)
    - No INSERT/UPDATE/DELETE by users

    Ranking_config:
    - SELECT: allow all authenticated (public config)
    - No INSERT/UPDATE/DELETE by users

    Ads_engagement:
    - SELECT own: `user_id = (select auth.uid())`
    - No direct modification by users

    Subscriptions:
    - SELECT own: `user_id = (select auth.uid())`
    - No direct modification by users (handled by payment webhooks)
  </action>
  <verify>
    <automated>
    Use Supabase MCP execute_sql to verify:
    1. SELECT count(*) FROM pg_proc WHERE proname IN ('is_blocked', 'shares_school') — should return 2
    2. SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public' AND rowsecurity = true — should list all 17 tables
    3. SELECT count(*) FROM pg_policies WHERE schemaname = 'public' — should be > 30 (multiple policies per table)
    </automated>
  </verify>
  <done>
    is_blocked() and shares_school() functions exist as SECURITY DEFINER STABLE. RLS is enabled on all 17 user-facing tables. Each table has appropriate SELECT/INSERT/UPDATE/DELETE policies. All policies use (select auth.uid()) wrapping for performance. Shared-school gating and block-check are enforced at the database level on profiles, photos, and user tables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate TypeScript types and update Supabase client</name>
  <files>
    src/types/database.types.ts
  </files>
  <action>
    1. Generate TypeScript types from the live Supabase schema using the MCP `generate_typescript_types` tool. Write the output to `src/types/database.types.ts`, replacing the placeholder created in Plan 01.

       If MCP unavailable, use CLI:
       ```bash
       npx supabase gen types typescript --project-id "PROJECT_ID" > src/types/database.types.ts
       ```

    2. Verify the generated types include all 17 tables and their column types.

    3. The Supabase client in `src/lib/supabase.ts` (from Plan 01) already imports from `@/types/database.types` — verify the import resolves correctly with the generated types.

    4. Run TypeScript compilation to confirm everything type-checks:
       ```bash
       npx tsc --noEmit
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    database.types.ts contains generated types for all 17 tables. Supabase client generic parameter `Database` resolves to the generated type. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `SELECT public.is_blocked('00000000-0000-0000-0000-000000000001'::uuid, '00000000-0000-0000-0000-000000000002'::uuid)` returns false (no blocks exist)
2. All 17 tables have RLS enabled (pg_tables.rowsecurity = true)
3. Policy count > 30 across all tables
4. `npx tsc --noEmit` passes
5. database.types.ts contains type definitions for all tables
6. Run Supabase security advisors (MCP get_advisors) — no critical issues
</verification>

<success_criteria>
- is_blocked() correctly detects bidirectional blocks
- shares_school() correctly detects shared school membership
- Both functions are SECURITY DEFINER and STABLE
- RLS enabled on all 17 tables
- Profiles/photos/users have shared-school + block-check SELECT policies
- Interaction tables (likes, matches, etc.) have own-data-only policies
- Messaging tables enforce thread participant checks
- TypeScript types generated and integrated with Supabase client
- No TypeScript compilation errors
- Supabase security advisor shows no critical missing RLS warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema/01-03-SUMMARY.md`
</output>
