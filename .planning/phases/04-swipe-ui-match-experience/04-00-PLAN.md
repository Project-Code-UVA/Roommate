---
phase: 04-swipe-ui-match-experience
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/hooks/use-discovery-stack.test.ts
  - __tests__/components/photo-indicator.test.ts
  - __tests__/components/match-modal.test.ts
  - __tests__/setup.ts
  - package.json
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, MTCH-02, MTCH-03]

must_haves:
  truths:
    - "Test stubs exist for all 6 phase requirements before implementation starts"
    - "Reanimated and Haptics mocks allow tests to run without native modules"
    - "@testing-library/react-native is available for component rendering tests"
  artifacts:
    - path: "__tests__/hooks/use-discovery-stack.test.ts"
      provides: "Test stubs for hook logic: swipe actions, stack management, pagination, match detection"
      contains: "it.todo"
    - path: "__tests__/components/photo-indicator.test.ts"
      provides: "Test stubs for photo index wrap and loop behavior"
      contains: "it.todo"
    - path: "__tests__/components/match-modal.test.ts"
      provides: "Test stubs for match modal rendering and action buttons"
      contains: "it.todo"
  key_links:
    - from: "__tests__/setup.ts"
      to: "react-native-reanimated"
      via: "jest.mock"
      pattern: "jest\\.mock.*reanimated"
---

<objective>
Wave 0: Test infrastructure for Phase 4 — install @testing-library/react-native, add reanimated/haptics mocks, create test stubs for all phase requirements.

Purpose: Ensure Nyquist compliance — every implementation task has a pre-existing test file to run against.
Output: 3 test stub files, updated mocks, @testing-library/react-native installed.
</objective>

<execution_context>
@/Users/bzcruz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bzcruz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-swipe-ui-match-experience/04-RESEARCH.md
@.planning/phases/04-swipe-ui-match-experience/04-VALIDATION.md

<interfaces>
<!-- Existing test setup pattern from Phase 2 -->
From __tests__/setup.ts:
- mockSession, resetAllMocks exports
- mockSupabase chainable query builder
- AsyncStorage in-memory mock

From src/types/filters.ts:
```typescript
export type DiscoveryProfile = {
  readonly user_id: string;
  readonly display_name: string;
  readonly bio: string | null;
  readonly year: string | null;
  readonly hometown: string | null;
  readonly nitty_gritty: NittyGritty | null;
  readonly completion_score: number;
  readonly mode_status: "roommate" | "friends";
  readonly selfie_verified: boolean;
  readonly last_active_at: string;
  readonly rank_score: number;
  readonly photos: readonly { readonly id: string; readonly url: string; readonly position: number }[];
};

export type LikeResult = {
  readonly success: boolean;
  readonly is_match: boolean;
  readonly match_id: string | null;
  readonly thread_id: string | null;
  readonly error: string | null;
};
```

From src/services/discovery-service.ts:
```typescript
export async function getDiscoveryStack(userId: string, limit?: number, offset?: number): Promise<DiscoveryStackResult>;
export async function dismissProfile(userId: string, dismissedId: string): Promise<{ error: string | null }>;
export async function saveProfile(userId: string, savedId: string): Promise<{ error: string | null }>;
export async function unsaveProfile(userId: string, savedId: string): Promise<{ error: string | null }>;
```

From src/services/match-service.ts:
```typescript
export async function likeProfile(likerId: string, likedId: string): Promise<LikeResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @testing-library/react-native and add reanimated/haptics mocks</name>
  <files>package.json, __tests__/setup.ts</files>
  <action>
1. Install @testing-library/react-native as dev dependency:
   `npx expo install @testing-library/react-native -- --save-dev`

2. Update `__tests__/setup.ts` to add mocks for react-native-reanimated and expo-haptics:

   Add `jest.mock('react-native-reanimated', () => require('react-native-reanimated/mock'))` at the top of setup.

   Add `jest.mock('expo-haptics', () => ({ impactAsync: jest.fn(), ImpactFeedbackStyle: { Heavy: 'heavy', Medium: 'medium', Light: 'light' }, notificationAsync: jest.fn(), selectionAsync: jest.fn() }))`.

   Add `jest.mock('expo-linear-gradient', () => { const { View } = require('react-native'); return { LinearGradient: View }; })`.

   Add `jest.mock('react-native-confetti-cannon', () => { const { View } = require('react-native'); return View; })` (will be needed once installed in Plan 01).

   Do NOT break existing mocks. Append new mocks after existing ones.

3. Verify existing tests still pass after adding mocks.
  </action>
  <verify>
    <automated>npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>@testing-library/react-native installed, reanimated/haptics/gradient mocks added, all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for all Phase 4 requirements</name>
  <files>__tests__/hooks/use-discovery-stack.test.ts, __tests__/components/photo-indicator.test.ts, __tests__/components/match-modal.test.ts</files>
  <action>
Create `__tests__/components/` directory if it does not exist.

Create `__tests__/hooks/use-discovery-stack.test.ts` with these stubs (all using `it.todo()`):

```
describe('useDiscoveryStack', () => {
  // Stack management
  it.todo('loads initial discovery stack on mount');
  it.todo('returns loading state while fetching');
  it.todo('returns empty state when stack has no profiles');
  it.todo('returns error state on fetch failure');

  // Swipe actions — DISC-01
  it.todo('dismissProfile removes top card from stack');
  it.todo('dismissProfile calls discovery service dismissProfile');
  it.todo('dismissProfile handles service errors gracefully');

  // Like actions — DISC-02
  it.todo('likeProfile removes top card from stack');
  it.todo('likeProfile calls match service likeProfile');
  it.todo('likeProfile handles service errors gracefully');

  // Save/bookmark — DISC-03
  it.todo('saveProfile calls discovery service saveProfile');
  it.todo('unsaveProfile calls discovery service unsaveProfile');
  it.todo('save toggle updates saved state for current profile');

  // Match detection — MTCH-02
  it.todo('sets matchData when likeProfile returns is_match true');
  it.todo('does not set matchData when likeProfile returns is_match false');
  it.todo('clears matchData when dismissMatch is called');

  // Thread auto-creation — MTCH-03
  it.todo('stores thread_id from likeProfile match response');

  // Pagination
  it.todo('fetches next page when stack length <= 5');
  it.todo('does not fetch when already loading');
  it.todo('appends new profiles to existing stack immutably');

  // Current profile
  it.todo('currentProfile returns first item in stack');
  it.todo('currentProfile returns null when stack is empty');
});
```

Create `__tests__/components/photo-indicator.test.ts` with these stubs:

```
describe('PhotoIndicator', () => {
  // DISC-04 — Photo carousel navigation
  it.todo('renders correct number of indicator segments');
  it.todo('highlights active segment based on current index');
  it.todo('non-active segments have reduced opacity style');
});

describe('Photo navigation logic', () => {
  // DISC-04 — Tap zone navigation with loop
  it.todo('tapping right half increments photo index');
  it.todo('tapping left half decrements photo index');
  it.todo('tapping right on last photo loops to first (index 0)');
  it.todo('tapping left on first photo loops to last');
  it.todo('works correctly with single photo (no change on tap)');
});
```

Create `__tests__/components/match-modal.test.ts` with these stubs:

```
describe('MatchModal', () => {
  // MTCH-02 — Match celebration modal
  it.todo('renders when visible prop is true');
  it.todo('does not render when visible prop is false');
  it.todo('displays both user profile photos');
  it.todo('displays "It\'s a Match!" heading');
  it.todo('renders Send a Message button');
  it.todo('renders Keep Swiping button');
  it.todo('renders Share button');
  it.todo('calls onSendMessage when Send a Message pressed');
  it.todo('calls onKeepSwiping when Keep Swiping pressed');
  it.todo('calls onShare when Share pressed');
});
```

All stubs use `it.todo()` pattern established in Phase 2/3. Import statements should reference the files that will be created:
- `import { useDiscoveryStack } from '@/hooks/use-discovery-stack'`
- `import { PhotoIndicator } from '@/components/discovery/photo-indicator'`
- `import { MatchModal } from '@/components/match/match-modal'`

Comment out the imports with `// TODO: uncomment when implemented` so the stubs pass without the source files existing.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="use-discovery-stack|photo-indicator|match-modal" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>3 test stub files created with total ~35 todo stubs covering DISC-01 through DISC-04, MTCH-02, MTCH-03. All stubs show as todo in test output. Existing tests unaffected.</done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage` — all existing + new stub tests pass
- `__tests__/components/` directory exists with 2 test files
- `__tests__/hooks/use-discovery-stack.test.ts` exists with ~22 todo stubs
- New mocks in setup.ts don't break existing test suite
</verification>

<success_criteria>
- @testing-library/react-native installed
- react-native-reanimated, expo-haptics, expo-linear-gradient mocked in setup.ts
- 3 test stub files with ~35 total it.todo() stubs
- All stubs map to DISC-01, DISC-02, DISC-03, DISC-04, MTCH-02, MTCH-03
- `npx jest --no-coverage` passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/04-swipe-ui-match-experience/04-00-SUMMARY.md`
</output>
