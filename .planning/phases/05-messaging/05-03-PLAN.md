---
phase: 05-messaging
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/chat/message-long-press.tsx
  - src/components/chat/gif-search-panel.tsx
  - src/components/chat/photo-preview.tsx
  - src/components/chat/chat-header.tsx
  - __tests__/components/chat/message-long-press.test.tsx
  - __tests__/components/chat/gif-search-panel.test.tsx
  - __tests__/components/chat/chat-header.test.tsx
autonomous: true
requirements: [MSG-04, MSG-05, MSG-06, MSG-07, MSG-08]

must_haves:
  truths:
    - "Long-press overlay shows 6 quick reaction emojis plus '+' for picker"
    - "Long-press overlay shows Reply, Copy text, Delete for me, Report message actions"
    - "GIF search panel shows search bar and trending/search results grid"
    - "Tapping a GIF calls onSelectGif immediately"
    - "Photo preview shows selected image with optional caption field"
    - "Chat header shows avatar, name, and overflow menu with Block/Report"
  artifacts:
    - path: "src/components/chat/message-long-press.tsx"
      provides: "Long-press overlay with reactions and actions"
      min_lines: 60
    - path: "src/components/chat/gif-search-panel.tsx"
      provides: "GIPHY search panel"
      min_lines: 50
    - path: "src/components/chat/chat-header.tsx"
      provides: "Chat header with overflow menu"
      min_lines: 40
  key_links:
    - from: "src/components/chat/gif-search-panel.tsx"
      to: "src/hooks/use-gif-search.ts"
      via: "hook consumption"
      pattern: "useGifSearch"
    - from: "src/components/chat/chat-header.tsx"
      to: "overflow menu"
      via: "Block/Report actions"
      pattern: "onPressBlock|onPressReport"
---

<objective>
Interaction components: long-press message menu, GIF search panel, photo preview, and chat header with overflow menu.

Purpose: Build the rich interaction overlays and panels that enable reactions, replies, media sharing, and safety actions. These components handle user gestures and surface action options per the locked design decisions.

Output: 4 component files with tests.
</objective>

<execution_context>
@/Users/bzcruz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bzcruz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-messaging/05-CONTEXT.md
@.planning/phases/05-messaging/05-RESEARCH.md
@.planning/phases/05-messaging/05-01-SUMMARY.md
@src/types/chat.ts
@src/hooks/use-gif-search.ts
</context>

<interfaces>
From src/types/chat.ts:
```typescript
export type Message = { id, body, sender_id, media_url, ... }
export type GifResult = { id, url, previewUrl, width, height }
export type ThreadUser = { id, display_name, avatar_url }
export type ReportCategory = 'harassment' | 'sexual_content' | ... | 'other'
```

From src/hooks/use-gif-search.ts:
```typescript
export function useGifSearch(): { query: string; results: readonly GifResult[]; isLoading: boolean; search: (term: string) => void }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Long-press overlay and GIF search panel</name>
  <files>
    src/components/chat/message-long-press.tsx,
    src/components/chat/gif-search-panel.tsx,
    __tests__/components/chat/message-long-press.test.tsx,
    __tests__/components/chat/gif-search-panel.test.tsx
  </files>
  <behavior>
    - LongPressMenu renders a semi-transparent backdrop overlay
    - LongPressMenu shows the target message bubble (visual reference)
    - LongPressMenu shows row of 6 quick emoji reactions: heart, laugh, thumbs-up, surprised, sad, fire
    - LongPressMenu shows '+' button that calls onOpenEmojiPicker
    - LongPressMenu shows "Reply" action row with icon
    - LongPressMenu shows "Copy text" action row with icon (only when message has body)
    - LongPressMenu shows "Delete for me" action row with icon
    - LongPressMenu shows "Report message" action row with icon
    - LongPressMenu calls onReact(emoji) when quick emoji tapped
    - LongPressMenu calls onReply when Reply tapped
    - LongPressMenu calls onCopy when Copy text tapped
    - LongPressMenu calls onDelete when Delete for me tapped
    - LongPressMenu calls onReport when Report message tapped
    - LongPressMenu calls onClose when backdrop tapped
    - GifSearchPanel renders TextInput search bar at top
    - GifSearchPanel renders grid of GIF previews (2-column masonry or grid)
    - GifSearchPanel calls useGifSearch hook for data
    - GifSearchPanel calls onSelectGif(gifResult) when a GIF tapped
    - GifSearchPanel shows loading state while searching
    - GifSearchPanel shows trending GIFs initially (on mount)
  </behavior>
  <action>
**src/components/chat/message-long-press.tsx:**
Props: visible (boolean), message (Message | null), onReact(emoji: string), onReply, onCopy, onDelete, onReport, onOpenEmojiPicker, onClose
- Use Modal with transparent background, or absolute positioned overlay
- Backdrop: `bg-black/50` Pressable that calls onClose
- Content: positioned near the message (center of screen is fine for MVP)
- Quick reactions row: 6 emojis as Pressable circles: ["❤️", "😂", "👍", "😮", "😢", "🔥"] + "+" button
- Each emoji in a `w-10 h-10 bg-white rounded-full items-center justify-center` Pressable
- Action list below reactions: each action is a row with Ionicons icon + text label
  - "Reply" with 'arrow-undo-outline'
  - "Copy text" with 'copy-outline' (only if message.body)
  - "Delete for me" with 'trash-outline'
  - "Report message" with 'flag-outline'
- Each action row: `flex-row items-center gap-3 py-3 px-4`
- Background: `bg-white rounded-2xl` with shadow
- Haptic feedback (expo-haptics impactLight) when opening

**src/components/chat/gif-search-panel.tsx:**
Props: onSelectGif(gif: GifResult), onClose, height (number -- should match keyboard height for smooth swap)
- Use useGifSearch hook internally
- Top: search TextInput with `bg-gray-100 rounded-full px-4 py-2` and search icon
- Grid: FlatList with numColumns={2}, renderItem shows Image with previewUrl
- Each GIF cell: Pressable, rounded corners, fixed width (half panel minus gaps), height computed from aspect ratio
- Loading: ActivityIndicator in center
- "Powered by GIPHY" attribution text at bottom (GIPHY ToS requirement)
- On search input change: call search(text) from hook (debounced internally)

Update both test files from stubs to real tests.
  </action>
  <verify>
    <automated>npx jest __tests__/components/chat/message-long-press.test.tsx __tests__/components/chat/gif-search-panel.test.tsx --no-coverage -x</automated>
  </verify>
  <done>LongPressMenu shows 6 emojis and 4 action buttons. All callbacks fire correctly. GifSearchPanel renders search and grid from useGifSearch hook. Both test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Photo preview and chat header with overflow menu</name>
  <files>
    src/components/chat/photo-preview.tsx,
    src/components/chat/chat-header.tsx,
    __tests__/components/chat/chat-header.test.tsx
  </files>
  <behavior>
    - PhotoPreview renders selected image with rounded corners
    - PhotoPreview shows optional caption TextInput below image
    - PhotoPreview shows Send button and Cancel button
    - PhotoPreview calls onSend(uri, caption?) when Send pressed
    - PhotoPreview calls onCancel when Cancel pressed
    - ChatHeader renders small circular avatar (32px) and display name
    - ChatHeader calls onPressProfile when avatar or name tapped
    - ChatHeader shows overflow (...) menu button on right
    - ChatHeader overflow menu shows "Block" and "Report" options
    - ChatHeader calls onPressBlock when Block selected
    - ChatHeader calls onPressReport when Report selected
    - ChatHeader shows back navigation arrow on left
    - ChatHeader calls onBack when back pressed
  </behavior>
  <action>
**src/components/chat/photo-preview.tsx:**
Props: uri (string), onSend(uri: string, caption?: string), onCancel
- Render above the composer (positioned absolutely or as a conditional component)
- Image with rounded corners, max-height ~300px, centered
- Caption TextInput below: optional, placeholder "Add a caption...", same style as composer input
- Row of buttons: Cancel (text, gray) and Send (primary-600 pill button with send icon)
- No test file needed (visual-only, simple presentational -- tested via integration in chat screen)

**src/components/chat/chat-header.tsx:**
Props: otherUser (ThreadUser), onPressProfile, onPressBlock, onPressReport, onBack
- SafeAreaView at top
- Row layout: back arrow | avatar + name | overflow button
- Back: Ionicons 'chevron-back' Pressable, calls onBack
- Avatar: Image 32x32 rounded-full, fallback to Ionicons 'person-circle'
- Name: text-base font-semibold, truncated with numberOfLines={1}
- Tapping avatar or name: calls onPressProfile
- Overflow: Ionicons 'ellipsis-horizontal' Pressable
- Overflow menu: use a simple dropdown (absolute positioned View with bg-white shadow, or use a state toggle). Two options:
  - "Block" with Ionicons 'ban-outline' in red
  - "Report" with Ionicons 'flag-outline'
- Tapping outside overflow menu closes it
- No online/last active status (per user decision: display name only)

Update chat-header test file from stub to real tests. Photo preview is visual-only and will be verified via screen integration.
  </action>
  <verify>
    <automated>npx jest __tests__/components/chat/chat-header.test.tsx --no-coverage -x</automated>
  </verify>
  <done>ChatHeader renders avatar, name, back button, overflow menu with Block/Report. PhotoPreview renders image with caption and send/cancel. Chat header test passes.</done>
</task>

</tasks>

<verification>
- All 4 component files exist in src/components/chat/
- Long-press menu has 6 quick emojis + 4 action items per user decision
- GIF panel uses useGifSearch hook and renders grid
- Chat header has avatar + name only (no online status) per user decision
- Chat header overflow has Block + Report per user decision
- Photo preview has optional caption per user decision
- npx jest --testPathPattern="message-long-press|gif-search-panel|chat-header" --no-coverage passes
</verification>

<success_criteria>
- Long-press shows reaction row at top, then actions (Reply, Copy, Delete, Report) per user decision
- Multiple reactions per user allowed per user decision
- GIF panel has search bar + trending grid per user decision
- Tap GIF to send immediately per user decision
- Photo preview has optional caption per user decision
- Chat header: avatar + name, no online/active status per user decision
- Block/Report in header overflow and long-press per user decision
</success_criteria>

<output>
After completion, create `.planning/phases/05-messaging/05-03-SUMMARY.md`
</output>
