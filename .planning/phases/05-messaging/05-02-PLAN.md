---
phase: 05-messaging
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/chat/message-bubble.tsx
  - src/components/chat/delivery-indicator.tsx
  - src/components/chat/date-separator.tsx
  - src/components/chat/message-reactions.tsx
  - src/components/chat/message-reply-preview.tsx
  - src/components/chat/message-list.tsx
  - src/components/chat/message-composer.tsx
  - src/components/chat/icebreaker-card.tsx
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-04, MSG-05, MSG-09]

must_haves:
  truths:
    - "Message bubbles render iMessage-style with sender purple/right and receiver gray/left"
    - "Last message in a cluster shows a tail"
    - "Timestamps appear between message groups (5-min threshold)"
    - "Date separators show Today/Yesterday/formatted date"
    - "Delivery indicators show single/double/blue-double checkmarks"
    - "Reaction pills render below bubbles with emoji and count"
    - "Reply preview shows quoted text above the reply bubble"
    - "Icebreaker card shows 3 prompts, dismissible, fills composer on tap"
    - "Composer has text input, send button, camera icon, GIF icon"
  artifacts:
    - path: "src/components/chat/message-bubble.tsx"
      provides: "iMessage-style bubble with tail"
      min_lines: 60
    - path: "src/components/chat/message-list.tsx"
      provides: "Inverted FlatList with grouping"
      min_lines: 80
    - path: "src/components/chat/message-composer.tsx"
      provides: "Text input with send/camera/GIF buttons"
      min_lines: 50
    - path: "src/components/chat/icebreaker-card.tsx"
      provides: "Floating prompt card"
      min_lines: 40
  key_links:
    - from: "src/components/chat/message-list.tsx"
      to: "src/components/chat/message-bubble.tsx"
      via: "renderItem prop"
      pattern: "MessageBubble"
    - from: "src/components/chat/message-bubble.tsx"
      to: "src/components/chat/delivery-indicator.tsx"
      via: "component composition"
      pattern: "DeliveryIndicator"
    - from: "src/components/chat/message-bubble.tsx"
      to: "src/components/chat/message-reactions.tsx"
      via: "component composition"
      pattern: "MessageReactions"
---

<objective>
Core chat UI components: message bubbles, message list, composer, delivery indicators, date separators, reactions display, reply previews, and icebreaker card.

Purpose: Build the visual building blocks of the chat screen. These components are stateless/presentational, receiving data and callbacks as props. They implement the locked iMessage-style design decisions from CONTEXT.md.

Output: 8 component files with NativeWind styling, all rendering correctly in tests.
</objective>

<execution_context>
@/Users/bzcruz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bzcruz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-messaging/05-CONTEXT.md
@.planning/phases/05-messaging/05-RESEARCH.md
@.planning/phases/05-messaging/05-00-SUMMARY.md
@.planning/phases/05-messaging/05-01-SUMMARY.md
@src/types/chat.ts
@src/lib/constants.ts
@src/lib/icebreaker-prompts.ts
</context>

<interfaces>
<!-- Types from Plan 00, hooks/services from Plan 01 -->

From src/types/chat.ts:
```typescript
export type Message = { id, thread_id, sender_id, body, media_url, reply_to_id, delivered_at, read_at, created_at, _status, reply_to }
export type MessageReaction = { id, message_id, user_id, emoji, created_at }
export type DeliveryStatus = 'sending' | 'sent' | 'delivered' | 'read'
export type IcebreakerPrompt = { id: string; text: string; category: 'roommate' | 'social' }
```

From src/lib/constants.ts:
```typescript
export const COLORS = { primary: { 500: "#8b5cf6", 600: "#7c3aed", ... }, gray: { 100: "#f3f4f6", 200: "#e5e7eb", ... } }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic components — bubble, delivery indicator, date separator, reactions, reply preview</name>
  <files>
    src/components/chat/message-bubble.tsx,
    src/components/chat/delivery-indicator.tsx,
    src/components/chat/date-separator.tsx,
    src/components/chat/message-reactions.tsx,
    src/components/chat/message-reply-preview.tsx,
    __tests__/components/chat/message-bubble.test.tsx,
    __tests__/components/chat/delivery-indicator.test.tsx,
    __tests__/components/chat/date-separator.test.tsx,
    __tests__/components/chat/message-reactions.test.tsx,
    __tests__/components/chat/message-reply-preview.test.tsx
  </files>
  <behavior>
    - MessageBubble renders sender bubble on right with purple-600 background
    - MessageBubble renders receiver bubble on left with gray-200 background
    - MessageBubble shows tail SVG/view on last message in cluster (isSender + isLast props)
    - MessageBubble renders body text in white (sender) or gray-900 (receiver)
    - MessageBubble renders media_url as Image with rounded corners (same width constraints as text)
    - MessageBubble shows DeliveryIndicator below text on sender messages only
    - MessageBubble shows MessageReactions below the bubble when reactions exist
    - MessageBubble shows MessageReplyPreview above the bubble when reply_to exists
    - MessageBubble calls onLongPress prop on long press gesture
    - MessageBubble calls onImagePress prop when media image tapped
    - DeliveryIndicator renders single Ionicons checkmark for 'sent'
    - DeliveryIndicator renders double checkmark for 'delivered'
    - DeliveryIndicator renders blue double checkmark for 'read'
    - DeliveryIndicator renders clock icon for 'sending'
    - DeliveryIndicator renders red exclamation for 'failed'
    - DateSeparator renders "Today" for today's date
    - DateSeparator renders "Yesterday" for yesterday
    - DateSeparator renders "Mar 9" format for older dates this year
    - DateSeparator renders "Mar 9, 2025" for dates in previous years
    - MessageReactions renders a row of pills, each pill shows emoji + count
    - MessageReactions calls onReactionPress(emoji) when pill tapped
    - MessageReplyPreview renders quoted text truncated at 100 chars
    - MessageReplyPreview shows sender name of quoted message
    - MessageReplyPreview calls onPress prop when tapped (to scroll to original)
  </behavior>
  <action>
**src/components/chat/message-bubble.tsx:**
Props: message (Message), isSender (boolean), isLastInCluster (boolean), reactions (readonly MessageReaction[]), onLongPress, onImagePress, onReactionPress, onReplyPress
- Use NativeWind: sender gets `bg-violet-600 self-end rounded-2xl rounded-br-sm` (tail effect via smaller border radius on the tail corner when isLastInCluster), receiver gets `bg-gray-200 self-start rounded-2xl rounded-bl-sm`
- Text: sender `text-white`, receiver `text-gray-900`
- Max width: 75% of screen (use `max-w-[75%]`)
- If media_url: render Image with `rounded-xl` and `w-56 h-56` (aspect ratio maintained), Pressable for onImagePress
- If reply_to exists: render MessageReplyPreview above the bubble content
- If reactions.length > 0: render MessageReactions below the bubble
- If isSender: render DeliveryIndicator with computed status from message._status/delivered_at/read_at
- Wrap in Pressable with onLongPress

**src/components/chat/delivery-indicator.tsx:**
Props: status (DeliveryStatus | 'sending' | 'failed')
- 'sending': Ionicons 'time-outline' in gray-400
- 'sent': Ionicons 'checkmark' in gray-400
- 'delivered': Two Ionicons 'checkmark' overlapped (or 'checkmark-done-outline') in gray-400
- 'read': Same double checkmark in primary-500 (blue-ish purple)
- 'failed': Ionicons 'alert-circle' in red-500
- Size: 14px icons, inline right-aligned below message text

**src/components/chat/date-separator.tsx:**
Props: date (string -- ISO timestamp)
- Parse date, compare to today/yesterday
- Render centered text with gray-500 color, text-xs, py-2
- "Today", "Yesterday", or formatted date (use Intl.DateTimeFormat or manual formatting)

**src/components/chat/message-reactions.tsx:**
Props: reactions (readonly MessageReaction[]), onReactionPress (emoji: string) => void
- Group reactions by emoji, count each
- Render horizontal row of pills: each pill has `bg-gray-100 rounded-full px-2 py-0.5 flex-row items-center gap-1`
- Each pill shows emoji text + count number
- Pressable: calls onReactionPress(emoji) to toggle

**src/components/chat/message-reply-preview.tsx:**
Props: originalMessage ({ body: string | null, sender_name: string }), onPress: () => void
- Render: left purple border (2px), gray-100 background, rounded
- Sender name in bold text-xs
- Body text truncated at 100 chars with ellipsis, text-xs text-gray-500
- Pressable: onPress scrolls to original message

Update all 5 test files from todo stubs to real tests using @testing-library/react-native render + screen queries.
  </action>
  <verify>
    <automated>npx jest __tests__/components/chat/message-bubble.test.tsx __tests__/components/chat/delivery-indicator.test.tsx __tests__/components/chat/date-separator.test.tsx __tests__/components/chat/message-reactions.test.tsx __tests__/components/chat/message-reply-preview.test.tsx --no-coverage -x</automated>
  </verify>
  <done>All 5 atomic components render correctly. Sender/receiver styling differs. Delivery indicators show correct icons per status. Date separators format Today/Yesterday/date. Reactions show grouped pills. Reply preview shows truncated quoted text. All 5 test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composite components — message list, composer, icebreaker card</name>
  <files>
    src/components/chat/message-list.tsx,
    src/components/chat/message-composer.tsx,
    src/components/chat/icebreaker-card.tsx,
    __tests__/components/chat/message-list.test.tsx,
    __tests__/components/chat/message-composer.test.tsx,
    __tests__/components/chat/icebreaker-card.test.tsx
  </files>
  <behavior>
    - MessageList renders inverted FlatList of messages
    - MessageList inserts date separators between messages on different days
    - MessageList groups consecutive messages from same sender within 5-min threshold
    - MessageList shows timestamp between message groups from same sender after 5+ min gap
    - MessageList calls onEndReached for pagination
    - MessageList passes isSender and isLastInCluster to each MessageBubble
    - MessageComposer renders TextInput with placeholder "Type a message..."
    - MessageComposer send button disabled when input empty
    - MessageComposer calls onSend(text) and clears input on send
    - MessageComposer shows camera icon button that calls onCameraPress
    - MessageComposer shows GIF icon button that calls onGifPress
    - MessageComposer shows reply preview above input when replyTo prop is set
    - MessageComposer reply preview has dismiss (X) button
    - MessageComposer TextInput is multiline with max height ~120px
    - IcebreakerCard renders 3 random prompts from pool
    - IcebreakerCard calls onSelectPrompt(text) when prompt tapped
    - IcebreakerCard has X dismiss button that calls onDismiss
    - IcebreakerCard has "More" button that rotates to next 3 prompts
    - IcebreakerCard renders as floating card centered in chat area
  </behavior>
  <action>
**src/components/chat/message-list.tsx:**
Props: messages (readonly Message[]), currentUserId (string), reactions (Map or grouped by messageId), onLongPress(message), onImagePress(mediaUrl), onReactionPress(messageId, emoji), onReplyPress(messageId), onEndReached, isLoading, ListEmptyComponent
- Use FlatList with inverted={true}
- keyExtractor: message.id
- Data preprocessing: compute message groups (same sender within 5-min threshold), insert DateSeparator items for date boundaries. Use a union type for list items: { type: 'message', data: Message, isSender, isLastInCluster } | { type: 'date', date: string } | { type: 'time', time: string }
- renderItem: switch on type, render MessageBubble for messages, DateSeparator for dates
- onEndReached for loading more (with onEndReachedThreshold={0.3})
- ListFooterComponent (appears at top visually due to inverted): loading spinner when fetching older messages

**src/components/chat/message-composer.tsx:**
Props: onSend(text: string), onCameraPress, onGifPress, replyTo (Message | null), onDismissReply, disabled (boolean)
- KeyboardAvoidingView wrapper with behavior="padding" on iOS
- Row layout: camera icon | GIF icon | TextInput | send button
- TextInput: multiline, maxHeight 120, placeholder "Type a message...", border rounded-full, bg-gray-100
- Send button: Ionicons 'send' in primary-600, disabled when text empty (gray-300)
- Camera button: Ionicons 'camera-outline'
- GIF button: Text "GIF" in a small rounded pill
- If replyTo: render compact reply preview above the input row with X dismiss button
- SafeAreaView for bottom inset

**src/components/chat/icebreaker-card.tsx:**
Props: onSelectPrompt(text: string), onDismiss
- Use getRandomPrompts(3) from @/lib/icebreaker-prompts on initial render
- State: currentPrompts, dismissed
- If dismissed: return null
- Render: centered floating card with bg-white rounded-2xl shadow-lg p-4
- X button in top-right corner (calls onDismiss)
- Title: "Break the ice" in bold
- 3 prompt buttons: each is a Pressable with bg-gray-50 rounded-xl p-3, text the prompt
- "More" button at bottom: text-primary-600, on press: getRandomPrompts(3) with new random set
- Tapping a prompt calls onSelectPrompt(prompt.text)

Update all 3 test files from stubs to real tests.
  </action>
  <verify>
    <automated>npx jest __tests__/components/chat/message-list.test.tsx __tests__/components/chat/message-composer.test.tsx __tests__/components/chat/icebreaker-card.test.tsx --no-coverage -x</automated>
  </verify>
  <done>MessageList renders inverted with date separators and message grouping. Composer has text input, send, camera, GIF buttons. Icebreaker shows prompts and is dismissible. All 3 test files pass.</done>
</task>

</tasks>

<verification>
- All 8 component files exist in src/components/chat/
- All 8 component test files pass
- MessageBubble shows purple/gray sender/receiver distinction
- MessageList inserts date separators and groups messages correctly
- Composer has all required action buttons (send, camera, GIF)
- Icebreaker card shows 3 prompts with More/dismiss functionality
- npx jest --testPathPattern="components/chat" --no-coverage passes
</verification>

<success_criteria>
- iMessage-style bubbles with purple sender / gray receiver per user decision
- Tails on last message in cluster per user decision
- WhatsApp-style checkmarks per user decision
- Timestamps between groups (not every message) per user decision
- Icebreaker card is floating, dismissible, with More button per user decision
- Composer has camera + GIF buttons per user decision
</success_criteria>

<output>
After completion, create `.planning/phases/05-messaging/05-02-SUMMARY.md`
</output>
