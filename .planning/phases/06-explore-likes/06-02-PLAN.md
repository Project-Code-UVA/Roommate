---
phase: 06-explore-likes
plan: 02
type: execute
wave: 2
depends_on: ["06-00"]
files_modified:
  - src/services/likes-service.ts
  - src/hooks/use-likes.ts
  - src/components/likes/liked-me-card.tsx
  - src/components/likes/matches-row.tsx
  - src/components/likes/my-likes-card.tsx
  - src/components/likes/upgrade-banner.tsx
  - app/(tabs)/likes.tsx
  - app/(tabs)/_layout.tsx
autonomous: true
requirements:
  - LIKE-01
  - LIKE-02
  - LIKE-03
  - LIKE-04

must_haves:
  truths:
    - "User can view Matches section with last message preview and unread indicator"
    - "User can view My Likes list of pending outbound likes"
    - "Free users see blurred Liked Me grid with no identifying info"
    - "Tapping blurred card or upgrade banner shows Coming Soon alert"
    - "Likes tab badge shows count of new people who liked you"
  artifacts:
    - path: "src/services/likes-service.ts"
      provides: "getMyLikes, getLikedMe, getLikedMeCount RPC wrappers"
      exports: ["getMyLikes", "getLikedMe", "getLikedMeCount"]
    - path: "src/hooks/use-likes.ts"
      provides: "Likes tab state management"
      exports: ["useLikes"]
    - path: "src/components/likes/liked-me-card.tsx"
      provides: "Blurred/revealed card for Liked Me section"
      exports: ["LikedMeCard"]
    - path: "src/components/likes/matches-row.tsx"
      provides: "Match row with photo, name, last message, unread"
      exports: ["MatchesRow"]
    - path: "app/(tabs)/likes.tsx"
      provides: "Complete Likes tab screen with scrollable sections"
      contains: "SectionList"
    - path: "app/(tabs)/_layout.tsx"
      provides: "Likes tab badge with liked-me count"
      contains: "LikesBadge"
  key_links:
    - from: "src/hooks/use-likes.ts"
      to: "src/services/likes-service.ts"
      via: "getMyLikes, getLikedMe calls"
      pattern: "getMyLikes|getLikedMe"
    - from: "src/hooks/use-likes.ts"
      to: "src/services/thread-service.ts"
      via: "getThreads for matches data"
      pattern: "getThreads"
    - from: "app/(tabs)/_layout.tsx"
      to: "src/services/likes-service.ts"
      via: "getLikedMeCount for badge"
      pattern: "getLikedMeCount"
---

<objective>
Build the complete Likes tab: service layer, hook, 4 UI components, screen assembly, and tab badge.

Purpose: Users can view their matches (with last message and unread indicators), pending outbound likes, and a blurred grid of people who liked them (with upgrade prompt stub for Phase 9 monetization).
Output: 8 files — service, hook, 4 components, 1 screen, 1 layout update.
</objective>

<execution_context>
@/Users/bzcruz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bzcruz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-explore-likes/06-CONTEXT.md
@.planning/phases/06-explore-likes/06-RESEARCH.md
@.planning/phases/06-explore-likes/06-00-SUMMARY.md

<interfaces>
<!-- Types from Plan 00 -->
From src/types/explore.ts:
```typescript
export type LikedMeProfile = {
  readonly user_id: string;
  readonly photo_url: string;
  readonly display_name: string | null;  // null for free users
  readonly liked_at: string;
};

export type MyLike = {
  readonly user_id: string;
  readonly display_name: string;
  readonly year: string | null;
  readonly photo_url: string;
  readonly liked_at: string;
};
```

<!-- Existing thread service for matches -->
From src/services/thread-service.ts:
```typescript
export type EnrichedThread = {
  id: string;
  match_id: string;
  status: ThreadStatus;
  created_at: string;
  other_user: ThreadUser;  // { id, display_name, photo_url }
  last_message: { content: string; created_at: string; sender_id: string } | null;
  unread_count: number;
};
export async function getThreads(userId: string): Promise<{ data: EnrichedThread[]; error: string | null }>;
export async function getTotalUnreadCount(userId: string): Promise<{ count: number; error: string | null }>;
```

<!-- Existing tab layout -->
From app/(tabs)/_layout.tsx:
```typescript
function UnreadBadge({ count }: { readonly count: number }) { ... }
// Already handles 99+ overflow, red badge styling
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Likes service and hook</name>
  <files>src/services/likes-service.ts, src/hooks/use-likes.ts</files>
  <behavior>
    likes-service:
    - getMyLikes(userId, limit, offset) calls get_my_likes RPC, returns { data: MyLike[], error }
    - getLikedMe(userId, limit, offset, isPaid) calls get_liked_me RPC, returns { data: LikedMeProfile[], error }
    - getLikedMeCount(userId) calls get_liked_me_count RPC, returns { count: number, error }
    - All functions return structured results, never throw

    use-likes:
    - Loads all three sections on mount: matches (via getThreads), myLikes, likedMe
    - matches: readonly EnrichedThread[] from thread-service
    - myLikes: readonly MyLike[] from likes-service
    - likedMe: readonly LikedMeProfile[] from likes-service
    - likedMeCount: number for badge
    - isLoading, isRefreshing states
    - refresh() reloads all three sections
    - Refreshes on tab focus (useFocusEffect)
    - isPaid: false (hardcoded stub, Phase 9 will add subscription check)
  </behavior>
  <action>
1. Create `src/services/likes-service.ts`:
   - Import supabase client, MyLike, LikedMeProfile types.
   - `getMyLikes(userId: string, limit = 24, offset = 0)` — calls `supabase.rpc('get_my_likes', { p_user_id, p_limit, p_offset })`. Parse jsonb result. Return `{ data: readonly MyLike[], error: string | null }`.
   - `getLikedMe(userId: string, limit = 24, offset = 0, isPaid = false)` — calls `supabase.rpc('get_liked_me', { p_user_id, p_limit, p_offset, p_is_paid: isPaid })`. Return `{ data: readonly LikedMeProfile[], error: string | null }`.
   - `getLikedMeCount(userId: string)` — calls `supabase.rpc('get_liked_me_count', { p_user_id })`. Return `{ count: number, error: string | null }`. On error, return count=0.
   - Never throw. Consistent `{ data/count, error }` pattern.

2. Create `src/hooks/use-likes.ts`:
   - Import getMyLikes, getLikedMe, getLikedMeCount from likes-service.
   - Import getThreads from thread-service.
   - Import useFocusEffect from expo-router, useCallback from react.
   - State: matches (EnrichedThread[]), myLikes (MyLike[]), likedMe (LikedMeProfile[]), likedMeCount (number), isLoading (boolean), isRefreshing (boolean).
   - `isPaid`: hardcoded `false`. Add `// TODO: Phase 9 — check subscription status` comment.
   - `loadAll()`: async function that fetches all three in parallel via Promise.all([getThreads(userId), getMyLikes(userId), getLikedMe(userId, 24, 0, isPaid), getLikedMeCount(userId)]). Sets state immutably.
   - useEffect on mount: call loadAll() with isLoading=true.
   - `refresh()`: set isRefreshing=true, call loadAll(), set isRefreshing=false.
   - useFocusEffect: call loadAll() on tab focus (but don't show loading spinner — silent refresh). Use useCallback to memoize.
   - Export `{ matches, myLikes, likedMe, likedMeCount, isLoading, isRefreshing, refresh, isPaid }`.

Uncomment test stub imports and implement tests.
  </action>
  <verify>
    <automated>npx jest __tests__/services/likes-service.test.ts __tests__/hooks/use-likes.test.ts --bail 2>&1 | tail -10</automated>
  </verify>
  <done>likes-service exports 3 functions. useLikes hook loads matches, myLikes, likedMe on mount and tab focus. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Likes UI components, screen assembly, and tab badge</name>
  <files>src/components/likes/liked-me-card.tsx, src/components/likes/matches-row.tsx, src/components/likes/my-likes-card.tsx, src/components/likes/upgrade-banner.tsx, app/(tabs)/likes.tsx, app/(tabs)/_layout.tsx</files>
  <behavior>
    liked-me-card:
    - Free: renders Image with BlurView overlay (intensity=60, tint="light"), no name visible, tap calls onUpgrade
    - Paid: renders Image with LinearGradient + name, tap calls onSelect

    matches-row:
    - Renders circular photo, display_name, last message text (truncated), relative time
    - Red dot unread indicator when unread_count > 0
    - Pressable navigates to chat screen

    my-likes-card:
    - Grid card with photo + name + year overlay (similar to ExploreGridCard)
    - Tap calls onPress

    upgrade-banner:
    - Yellow/gold banner text "See who likes you" with sparkle icon
    - "Upgrade" button that calls onUpgrade
    - onUpgrade shows Alert.alert("Coming Soon", "Premium features launching soon!")

    likes screen:
    - ScrollView with 3 sections: Matches, Liked Me (with banner), My Likes
    - Each section has header with title and count
    - Matches: vertical list of MatchesRow (or horizontal if < 5)
    - Liked Me: upgrade banner + 3-col grid of LikedMeCard
    - My Likes: 3-col grid of MyLikesCard
    - Pull-to-refresh on entire ScrollView
    - Empty states per section

    _layout.tsx:
    - Add getLikedMeCount polling (60s interval, not 30s)
    - Add LikesBadge to Likes tab icon (same style as UnreadBadge)
  </behavior>
  <action>
1. Create `src/components/likes/matches-row.tsx`:
   - Props: `{ thread: EnrichedThread; onPress: () => void }`.
   - Pressable row: 56px circular Image (other_user.photo_url), display_name (bold), last_message.content (gray, numberOfLines=1, truncated), relative time on right.
   - If unread_count > 0: red dot (8px circle) next to name, and last message text in bold.
   - If no last_message: show "New match! Say hi" in italic.
   - testID: `match-row-${thread.id}`.

2. Create `src/components/likes/liked-me-card.tsx`:
   - Props: `{ profile: LikedMeProfile; isPaid: boolean; onUpgrade: () => void; onSelect: (profile: LikedMeProfile) => void; size: number }`.
   - Pressable with fixed width/height = size.
   - Image fills card, `resizeMode="cover"`.
   - When `!isPaid`: overlay with `BlurView` from expo-blur, `intensity={60}`, `tint="light"`, `style={StyleSheet.absoluteFill}`. Tap calls onUpgrade.
   - When `isPaid` and `display_name` exists: LinearGradient overlay at bottom with display_name. Tap calls onSelect(profile).
   - Rounded corners (borderRadius: 8).
   - testID: `liked-me-card-${profile.user_id}`.

3. Create `src/components/likes/my-likes-card.tsx`:
   - Props: `{ like: MyLike; onPress: () => void; size: number }`.
   - Same layout as ExploreGridCard: Image + LinearGradient + name + year.
   - testID: `my-like-card-${like.user_id}`.

4. Create `src/components/likes/upgrade-banner.tsx`:
   - Props: `{ count: number; onUpgrade: () => void }`.
   - Horizontal banner: gold/amber background (bg-amber-50 border border-amber-200).
   - Left: sparkle icon (Ionicons "sparkles"), text "See who likes you" + count if > 0 ("N people liked you").
   - Right: "Upgrade" button (bg-amber-500, white text, rounded-full).
   - onUpgrade: `Alert.alert("Coming Soon", "Premium features launching soon!")`.
   - testID: `upgrade-banner`.

5. Replace `app/(tabs)/likes.tsx` stub:
   - Import useLikes hook, all 4 components, useSession.
   - Layout decision (Claude's discretion): Use ScrollView with 3 manual sections and sticky-style headers. Order: Matches (top), Liked Me (middle with upgrade banner), My Likes (bottom).
   - Section headers: bold title + count badge. E.g., "Matches (5)", "Liked Me (12)", "My Likes (3)".
   - Matches section:
     - If matches.length === 0: empty state "No matches yet. Keep exploring!"
     - Render MatchesRow for each match. Tap navigates to `/(tabs)/messages?threadId=${thread.id}` (or use router.push to chat screen).
   - Liked Me section:
     - UpgradeBanner at top (always shown for free users, count=likedMeCount).
     - 3-column grid of LikedMeCard. Use View with flexWrap/flexRow for grid within ScrollView (not nested FlatList).
     - If likedMe.length === 0 and likedMeCount === 0: empty state "No one has liked you yet."
   - My Likes section:
     - 3-column grid of MyLikesCard.
     - If myLikes.length === 0: empty state "You haven't liked anyone yet. Explore profiles!"
   - RefreshControl on ScrollView for pull-to-refresh.
   - Loading: show ActivityIndicator centered while isLoading on initial load.
   - SafeAreaView wrapper with "Likes" header title.

6. Update `app/(tabs)/_layout.tsx`:
   - Import getLikedMeCount from likes-service.
   - Add `likedMeCount` state (number, default 0).
   - Add fetchLikedMeCount callback, similar to fetchUnread but calls getLikedMeCount.
   - Poll every 60 seconds (not 30s — liked-me changes less frequently than messages).
   - Also call on mount via useEffect.
   - Add LikesBadge to Likes tab icon, reusing existing UnreadBadge component:
     ```
     <View>
       <Ionicons name={focused ? "heart" : "heart-outline"} ... />
       <UnreadBadge count={likedMeCount} />
     </View>
     ```

Uncomment test stub imports and implement tests alongside.

CRITICAL: MatchesRow navigation — when user taps a match, navigate to the chat screen for that thread. Use `router.push` from expo-router. The exact route depends on how messaging screens are structured (check Phase 05 for the chat route pattern).
  </action>
  <verify>
    <automated>npx jest --testPathPattern="likes" --bail 2>&1 | tail -10</automated>
  </verify>
  <done>Likes tab shows 3 sections: Matches with last message + unread, Liked Me with blur + upgrade banner, My Likes grid. Tab badge shows liked-me count. All Likes tests pass.</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="likes" --bail` — all Likes tests pass
- Likes tab renders 3 sections with correct data
- Blurred cards show no identifying info for free users
- Upgrade banner and blurred card tap show "Coming Soon" alert
- Matches show last message preview and unread indicator
- Tab badge updates with liked-me count
- `npx jest` — full suite still green
</verification>

<success_criteria>
- Matches section: photo + name + last message preview + unread dot
- My Likes: 3-column grid of pending outbound likes
- Liked Me: blurred 3-column grid with upgrade banner (free users)
- Upgrade stub shows "Coming Soon" alert
- Likes tab badge shows new liked-me count
- Badge polls every 60 seconds
- All Likes tests green
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-explore-likes/06-02-SUMMARY.md`
</output>
