---
phase: 02-auth-onboarding
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/setup.ts
  - __tests__/services/auth-service.test.ts
  - __tests__/services/photo-service.test.ts
  - __tests__/services/school-service.test.ts
  - __tests__/services/profile-service.test.ts
  - __tests__/hooks/use-onboarding.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-08]

must_haves:
  truths:
    - "Test stubs exist for all auth and onboarding services before implementation begins"
    - "Jest runs successfully with all stubs marked as pending/todo"
    - "Supabase and AsyncStorage mocks are shared across all test files"
  artifacts:
    - path: "__tests__/setup.ts"
      provides: "Shared test setup with Supabase and AsyncStorage mocks"
      min_lines: 20
    - path: "__tests__/services/auth-service.test.ts"
      provides: "Test stubs for age validation, OTP, onboarding completion"
      min_lines: 30
    - path: "__tests__/services/photo-service.test.ts"
      provides: "Test stubs for photo upload, delete, reorder"
      min_lines: 15
    - path: "__tests__/services/school-service.test.ts"
      provides: "Test stubs for school search, add, remove"
      min_lines: 15
    - path: "__tests__/services/profile-service.test.ts"
      provides: "Test stubs for profile update and retrieval"
      min_lines: 15
    - path: "__tests__/hooks/use-onboarding.test.ts"
      provides: "Test stubs for onboarding progress hook"
      min_lines: 15
  key_links: []
---

<objective>
Create the Wave 0 test infrastructure: shared mocks, test setup, and stub test files for all Phase 2 services and hooks.

Purpose: The Nyquist validation contract requires test files to exist before implementation tasks run. This plan creates all test stubs with pending/todo tests that will be filled in as services are implemented. It also establishes shared mocks (Supabase client, AsyncStorage) used across all test files.

Output: 6 test files with pending stubs, 1 shared setup file with mocks. All stubs pass `npx jest` (as todo/pending tests are not failures).
</objective>

<execution_context>
@/Users/bzcruz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bzcruz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-onboarding/02-CONTEXT.md
@.planning/phases/02-auth-onboarding/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared test setup and Supabase/AsyncStorage mocks</name>
  <files>__tests__/setup.ts</files>
  <action>
1. Create `__tests__/setup.ts` with shared test infrastructure:
   - Mock `@react-native-async-storage/async-storage` (use `@react-native-async-storage/async-storage/jest/async-storage-mock` if available, otherwise manual mock with get/set/remove/clear)
   - Mock `src/lib/supabase.ts` exporting a mock `supabase` object with chained methods:
     - `supabase.auth.signInWithOtp` → jest.fn() resolving `{ data: {}, error: null }`
     - `supabase.auth.verifyOtp` → jest.fn() resolving `{ data: { session: mockSession }, error: null }`
     - `supabase.auth.getSession` → jest.fn() resolving `{ data: { session: null }, error: null }`
     - `supabase.from(table).select/insert/update/delete/upsert` → chainable jest.fn() pattern
     - `supabase.storage.from(bucket).upload/remove/getPublicUrl` → jest.fn()
   - Export `mockSession` object with `{ user: { id: 'test-user-id' }, access_token: 'test-token' }` for reuse
   - Export `resetAllMocks()` helper that calls `jest.clearAllMocks()` and resets AsyncStorage

2. Ensure `jest` config in `package.json` includes `setupFiles` or `setupFilesAfterFramework` pointing to `__tests__/setup.ts` if not already configured. If the jest config uses `jest-expo` preset, add a `setupFiles: ['<rootDir>/__tests__/setup.ts']` entry.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=setup --no-coverage --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Shared test setup file exists with Supabase and AsyncStorage mocks. Jest config references the setup file.</done>
</task>

<task type="auto">
  <name>Task 2: Test stub files for all Phase 2 services and hooks</name>
  <files>__tests__/services/auth-service.test.ts, __tests__/services/photo-service.test.ts, __tests__/services/school-service.test.ts, __tests__/services/profile-service.test.ts, __tests__/hooks/use-onboarding.test.ts</files>
  <action>
1. **auth-service.test.ts** — stubs for AUTH-01, AUTH-02, AUTH-06:
   ```
   describe('auth-service', () => {
     describe('age validation', () => {
       it.todo('returns true for user exactly 18 years old');
       it.todo('returns true for user over 18');
       it.todo('returns false for user under 18');
       it.todo('returns false for user one day before 18th birthday');
     });
     describe('otp', () => {
       it.todo('sendOtp calls supabase.auth.signInWithOtp with formatted phone');
       it.todo('sendOtp returns error message on failure');
       it.todo('verifyOtp returns session on success');
       it.todo('verifyOtp returns error on invalid code');
     });
     describe('onboarding completion', () => {
       it.todo('createUserRecord inserts into users table');
       it.todo('markOnboardingComplete sets onboarding_completed to true');
     });
   });
   ```

2. **photo-service.test.ts** — stubs for AUTH-03:
   ```
   describe('photo-service', () => {
     it.todo('uploadPhoto uploads base64 to storage bucket');
     it.todo('uploadPhoto inserts row into photos table with order_index');
     it.todo('deletePhoto removes from storage and deletes photos row');
     it.todo('reorderPhotos updates order_index for each photo');
   });
   ```

3. **school-service.test.ts** — stubs for AUTH-04:
   ```
   describe('school-service', () => {
     it.todo('searchSchools queries with ilike filter');
     it.todo('searchSchools returns empty array for no matches');
     it.todo('addUserSchool inserts into user_schools');
     it.todo('removeUserSchool deletes from user_schools');
     it.todo('getUserSchools returns joined school data');
   });
   ```

4. **profile-service.test.ts** — stubs for AUTH-05:
   ```
   describe('profile-service', () => {
     it.todo('updateProfile upserts fields into profiles table');
     it.todo('getProfile returns profile data for user');
     it.todo('getProfile returns null for non-existent user');
   });
   ```

5. **use-onboarding.test.ts** — stubs for AUTH-08:
   ```
   describe('useOnboarding', () => {
     it.todo('saveProgress stores step data to AsyncStorage');
     it.todo('getProgress retrieves saved progress');
     it.todo('clearProgress removes all onboarding data');
     it.todo('getCurrentStepIndex returns correct index for current step');
     it.todo('ONBOARDING_STEPS contains all 8 steps in order');
   });
   ```

Each file should import from the setup file where needed. Use `it.todo()` (not `it.skip()`) so Jest reports them as pending, not skipped.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=auth --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 test stub files exist with todo tests. `npx jest --no-coverage` runs green (todo tests are not failures). Total: ~27 pending test stubs covering AUTH-01 through AUTH-08.</done>
</task>

</tasks>

<verification>
- `npx jest --no-coverage` runs without errors (all tests are todo/pending)
- All 6 files from VALIDATION.md Wave 0 Requirements exist
- Setup file exports shared mocks
</verification>

<success_criteria>
- All 6 test files listed in VALIDATION.md Wave 0 Requirements are created
- Jest runs successfully with pending test stubs
- Shared Supabase and AsyncStorage mocks are reusable across all test files
- Wave 0 is complete, unblocking Plans 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-onboarding/02-00-SUMMARY.md`
</output>
