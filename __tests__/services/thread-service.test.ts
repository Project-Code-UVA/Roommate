/**
 * Tests for thread-service.
 * Covers: Thread listing, single thread fetch, mark delivered
 */

// import { listThreads, getThread, markDelivered } from "@/services/thread-service";

describe("thread-service", () => {
  it.todo("lists user threads with last message preview");
  it.todo("gets single thread by id");
  it.todo("marks thread messages as delivered");
  it.todo("returns empty list when no threads");
});
