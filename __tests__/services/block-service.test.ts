/**
 * Tests for block-service.
 * Covers: MSG-07
 */

// import { blockFromChat } from "@/services/block-service";

describe("block-service", () => {
  it.todo("blocks user from chat (calls unmatchUser with blockToo=true)");
  it.todo("returns error on failure");
});
