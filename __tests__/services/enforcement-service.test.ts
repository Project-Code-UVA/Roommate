/**
 * Tests for enforcement-service.
 * Covers: SAFE-04, SAFE-05
 */

// import { resetAllMocks, mockSupabase } from "../setup";
// import { getEnforcementInfo, isActionBlocked } from "@/services/enforcement-service";

describe("enforcement-service", () => {
  describe("getEnforcementInfo", () => {
    it.todo("getEnforcementInfo returns none state for clean user");
    it.todo("getEnforcementInfo returns active enforcement with end date");
  });

  describe("isActionBlocked", () => {
    it.todo("isActionBlocked returns true for suspended user trying to like");
  });
});
