/**
 * Tests for use-chat-messages hook.
 * Covers: MSG-01 (realtime messaging)
 */

// import { useChatMessages } from "@/hooks/use-chat-messages";

describe("useChatMessages", () => {
  it.todo("fetches initial messages on mount");
  it.todo("subscribes to Realtime INSERT events");
  it.todo("skips self-sent messages from Realtime");
  it.todo("handles Realtime UPDATE for delivery status");
  it.todo("loads more messages on scroll");
  it.todo("cleans up subscription on unmount");
});
