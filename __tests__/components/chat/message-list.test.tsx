/**
 * Tests for MessageList component.
 * Covers: MSG-02 (timestamps, grouping)
 */

// import { MessageList } from "@/components/chat/message-list";

describe("MessageList", () => {
  it.todo("renders messages in inverted order");
  it.todo("shows date separators between groups");
  it.todo("shows timestamps between message clusters from same sender after 5+ min gap");
  it.todo("calls onEndReached for pagination");
  it.todo("renders empty state");
});
